apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kro
  namespace: kro
spec:
  interval: 24h
  chart:
    spec:
      chart: kro
      version: "0.7.0"
      sourceRef:
        kind: HelmRepository
        name: kro
        namespace: kro
      interval: 24h
